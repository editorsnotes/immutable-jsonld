{
  "name": "immutable-jsonld",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src",
    "transpile": "babel src -d lib --source-maps inline",
    "build": "npm -s run lint && npm -s run transpile",
    "transpile-tests": "npm -s run build && babel test -d lib/test",
    "test": "npm -s run transpile-tests && tape lib/test/*.js | tap-spec"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-eslint": "^5.0.0",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.3.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  },
  "dependencies": {
    "immutable": "^3.7.6"
  }
}
